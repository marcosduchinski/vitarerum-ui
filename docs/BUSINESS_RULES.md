Business Rule 01

A conversation starts with an email message.

When a conversation is created, a proposal is automatically created with the status `SUBMITTED`. The creation of the proposal generates a `ProposalEvent` of type `SUBMITTED`.

New proposals can be listed, assumed by the current user, forwarded to another user, or viewed in detail.
